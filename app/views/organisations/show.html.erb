<p style="float: right">
  <%= delete_button(@organisation) %>
</p>

<%= page_header(@organisation.name) %>

<p>
  Revenue per hour: <%= number_to_currency(@organisation.revenue_per_hour, unit: '£') %>
  <small>(invoiceable amount/total time over 2yr)</small>
</p>

<p><%= link_to 'I have recently contacted this organisation', contacted_organisation_path, data: { method: 'post' } %></p>

<h2>Users</h2>
<div class="list">
  <ul>
    <li><%= link_to 'Add', new_user_path(organisation_id: @organisation), class: 'btn' %></li>
    <% @organisation.users.each do |u| -%>
    <li><%= link_to u.name, u %></li>
    <% end -%>
  </ul>
</div>
<div class="row">
  <div class="span6">
<div class="tab-content">
    <h2>Email addresses</h2>
    <p>
      <%= link_to 'Add', new_email_address_path(organisation_id: @organisation), class: 'btn' %>
    </p>
<% unless @organisation.email_addresses.empty? -%>
    <table class="table table-bordered">
    <% @organisation.email_addresses.each do |a| -%>
      <tr>
        <td><%= mail_to a.address, a.address %></td>
        <td>
          <%= edit_button(a) %>
          <%= delete_button(a) %>
        </td>
      </tr>
    <% end -%>
    </table>
<% end -%>
  </div>
  <div class="span6">
    <h2>Contact numbers</h2>
  </div>
</div>

<h2>Addresses</h2>
<div class="list">
  <ul>
    <li><%= link_to 'Add', new_address_path(organisation_id: @organisation), class: 'btn' %></li>
    <% @organisation.addresses.each do |a| -%>
    <li><%= link_to format_address_single_line(a), edit_address_path(a) %></li>
    <% end -%>
  </ul>
</div>
<h2>Domains</h2>
<div class="list">
  <ul>
    <li><%= link_to 'Add', new_domain_path(organisation_id: @organisation), class: 'btn' %></li>
  <% @organisation.domains.each do |d| -%>
  <li><%= link_to d.name, domain_path(d) %></li>
  <% end -%>
  </ul>
</div>

<h2>Databases</h2>

<h2>To-do list</h2>

<p>
  <%= link_to 'Add', new_to_do_path(organisation_id: @organisation), class: 'btn' %>
</p>

<% unless @organisation.to_dos.empty? -%>
<%= render partial: 'to_dos/to_do_list', locals: {to_dos: @organisation.to_dos} %>
<% end -%>

  <div class="tab-pane" id="notes">
    <h2>Note Pads</h2>
    <p>
      <%= link_to 'Add', new_note_pad_path(organisation_id: @organisation), class: 'btn' %>
    </p>
    <table class="table table-bordered">
<% @organisation.note_pads.each do |np| -%>
      <tr>
        <td>
          <strong><%= np.title %></strong><br>
          <pre><%= np.content[0..255] %></pre>
        </td>
        <td><%= np.updated_at %></td>
        <td>
          <%= edit_button(np) %>
          <%= delete_button(np) %>
        </td>
      </tr>
<% end -%>
    </table>
  </div>
</div>
